
# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Allure (resultados y reporte generado, no se versionan)
/allure-results/
/allure-report/

# Newman (reportes sueltos si se usan otros reporters localmente)
/newman/

# Reportes HTML de API (Newman/htmlextra) y UI (Playwright) movidos por CI,
# generados por scripts/run-api-tests.js y por el Jenkinsfile (no se versionan)
/reports/

# Entornos locales
.env
.env.local
